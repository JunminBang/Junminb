# Unity 절차적 바이옴·다층 채굴 게임 구현 가이드

작성일: 2026-09-18

이 문서는 대화에서 논의한 구조와 조사한 Unity 자료를 구현 순서로 정리한 설계안이다. 코드 구현이나 성능 검증이 완료된 상태는 아니다. 맵 크기와 주기는 실험용 초깃값이다.

현재 파일이 저장된 작업 폴더는 Godot 프로젝트지만, 이 문서는 별도의 Unity 구현을 위한 가이드다. 기존 게임 코드와 엔진 설정은 변경하지 않는다.

## 1. 목표와 확정할 게임 규칙

- 마인크래프트처럼 월드마다 다른 바이옴·지형·자원 배치.
- 드워프 포트리스처럼 층을 선택하는 3/4 탑다운 2D 화면.
- 휠은 표시 높이만 변경한다. 주민의 실제 위치는 바뀌지 않는다.
- 주민은 연결된 계단을 통해서만 다른 층으로 이동한다.
- 지하는 주로 암석이며, 일부 구역은 생성 단계에서 자연 동굴로 비운다.
- 주민은 계단을 내려가 같은 층의 X/Y 방향으로 벽을 파서 통로를 만든다.
- 수평 채굴은 벽만 제거하고 바닥은 남긴다.
- 자연 동굴과 광맥은 탐사하기 전까지 가려진다.
- 같은 시드·설정·생성기 버전은 같은 초기 월드를 만든다.

```text
시드와 생성 설정
  → 고도·기후·바이옴
  → 지표·토양·지층·광맥
  → 자연 동굴
  → 플레이 가능성 검사
  → 월드 데이터
      ├─ 층 단면 표시
      ├─ 이동·계단·A* 길찾기
      ├─ 채굴·탐사·건설
      └─ 저장·불러오기
```

## 2. 첫 프로토타입 범위

| 항목 | 권장 시작점 |
|---|---|
| 규칙 테스트 맵 | 수동 배치 16 × 16칸 × 4층 |
| 첫 절차적 지역맵 | 64 × 64칸 × 32층 |
| 바이옴 | 숲·초원·사막·설원 |
| 주민 | 1명부터 시작 |
| 수평 이동 | 상하좌우 4방향 |
| 층 이동 | 같은 X/Y의 인접 층 계단 연결 |
| 동굴 | 우선 한 층 높이, 바닥 유지 |
| 작업 | 수평 채굴과 계단 굴착 |
| 후순위 | 물 흐름·붕괴·전투·다층 대동굴·무한 월드 |

처음에는 아트 대신 색 타일을 사용한다. 이동·채굴·층 전환이 작동한 뒤 절차적 생성과 3/4 아트를 붙인다.

## 3. 좌표와 칸 데이터

### 3.1 좌표 기준

- X/Y는 평면 위치, Z는 높이다. Z가 커지면 위층이다.
- `viewZ`는 화면의 표시 상한 높이다.
- `agentZ`는 주민이 실제로 있는 높이다.
- 논리 좌표와 Unity Transform 좌표는 변환 함수로 분리한다.
- 청크 내부 좌표와 월드 좌표도 구분한다.

배열 인덱스 예시:

```csharp
int Index(int x, int y, int z, int sizeX, int sizeY)
    => x + sizeX * (y + sizeY * z);
```

배열 접근 전에 좌표 범위를 검사한다.

### 3.2 벽과 바닥을 분리하는 모델

대화에서는 블록 위에 서는 모델과 벽·바닥을 분리하는 모델을 모두 언급했다. 구현에서는 혼용하지 않는다. 이 가이드는 수평 채굴을 표현하기 쉬운 **벽·바닥 분리 모델**을 사용한다.

주민은 `(x, y, z)` 칸의 바닥 위에 선다. 같은 칸에 벽이 있으면 진입할 수 없다. 보행 바닥을 찾기 위해 항상 `z-1`의 블록을 검사하는 다른 모델과 섞지 않는다.

| 칸 상태 | 이동 | 의미 |
|---|---|---|
| 벽 있음 + 바닥 있음 | 불가 | 채굴할 암석 |
| 벽 없음 + 바닥 있음 | 가능 | 지표·통로·동굴 바닥 |
| 벽 없음 + 바닥 없음 | 일반 보행 불가 | 수직 빈 공간 |
| 벽 없음 + 유효한 계단 | 가능 | 계단이 발판 역할을 함 |

수평 채굴은 벽만 제거한다. 바닥 제거는 별도 작업이며 첫 버전에서는 생략해도 된다. 여러 층 높이의 동굴을 만들 때만 중간층 바닥을 제거한다.

### 3.3 데이터 예시

다음은 데이터 구조 예시이며 완성된 게임 코드는 아니다.

```csharp
[System.Flags]
public enum CellFlags : byte
{
    None = 0,
    HasWall = 1 << 0,
    HasFloor = 1 << 1,
    StairUp = 1 << 2,
    StairDown = 1 << 3,
    Explored = 1 << 4
}

public struct CellData
{
    public ushort WallMaterial;
    public ushort FloorMaterial;
    public CellFlags Flags;
    public byte LiquidAmount;
    public ushort ExtraState;
}
```

- 위 필드 구성은 칸당 8바이트를 목표로 한다. 실제 런타임 크기는 측정한다.
- 재질 ID로 돌·흙·광석 정의와 스프라이트를 참조한다.
- 바이옴·지표 높이·기후는 주로 X/Y 열 단위로 저장한다.
- 아이템 목록·건축물·주민·작업은 별도 저장소에 둔다.
- 과거에 탐사했는지와 현재 시야에 보이는지는 분리한다.
- `ScriptableObject`는 재질·바이옴의 공통 정의에 사용하며 칸마다 만들지 않는다.

## 4. 절차적 월드 생성

### 4.1 시드와 재현성

전역 난수 발생기 하나를 모든 시스템에서 순서대로 소비하면 생성 순서 변화가 결과 전체에 영향을 줄 수 있다. 시스템과 구역별로 난수 흐름을 분리한다.

```text
지형 시드 = StableHash(월드 시드, 지형 ID, 구역 좌표)
동굴 시드 = StableHash(월드 시드, 동굴 ID, 구역 좌표)
광맥 시드 = StableHash(월드 시드, 광맥 ID, 구역 좌표)
```

- 안정적인 해시와 고정된 PRNG 알고리즘을 사용한다.
- 기본 문자열 `GetHashCode()`를 영구 월드 시드 규칙으로 사용하지 않는다.
- 생성기 버전과 설정을 저장한다. 시드가 같아도 알고리즘이 바뀌면 결과는 달라질 수 있다.
- 실행 환경 간 완전한 재현성이 필요하면 노이즈와 부동소수점 연산도 검증한다.

### 4.2 고도·기후·바이옴

1. 큰 규모의 노이즈로 지형 높낮이를 생성한다.
2. 작은 규모의 노이즈로 언덕과 세부 변화를 더한다.
3. 위도·고도·기후 노이즈로 온도를 구한다.
4. 강수량과 배수성을 생성한다.
5. 환경값 조합으로 바이옴을 결정한다.

| 환경 | 바이옴 예시 |
|---|---|
| 낮은 온도 | 설원·툰드라 |
| 높은 온도 + 적은 강수량 | 사막 |
| 적당한 온도 + 충분한 강수량 | 숲 |
| 중간 강수량 | 초원 |
| 많은 수분 + 낮은 배수성 | 습지, 후속 확장 |

칸마다 바이옴을 독립적으로 추첨하지 않는다. 인접 위치는 비슷한 환경값을 가져야 넓은 숲과 사막이 형성된다. 바이옴은 바닥 색뿐 아니라 목재·작물·물·동물 등 플레이 차이로 연결한다.

월드맵 선택 화면은 지역맵이 작동한 다음 추가한다. 이후 지역 환경은 월드맵의 값을 보간한 결과에 작은 규모의 노이즈를 더해 만든다. 월드 전체를 채굴 가능한 세밀한 칸으로 생성할 필요는 없다.

### 4.3 지표·지층·광맥

1. 각 X/Y 위치의 지표 높이 H를 구한다.
2. Z = H에는 벽 없는 지표 바닥을 둔다.
3. Z < H는 토양 깊이와 지층에 따라 벽·바닥 재질을 채운다.
4. Z > H는 기본적으로 벽·바닥 없는 공기다.
5. 광맥은 적합한 암석과 깊이에 연결된 덩어리 또는 가늘고 긴 형태로 배치한다.

지층 두께와 경계를 완만하게 변화시킨다. 지하 전체를 같은 돌로 채우거나 광물을 칸마다 독립 추첨하는 방식은 피한다.

### 4.4 자연 동굴

초기 권장 알고리즘은 **방 배치 → 연결할 방 선택 → 통로 생성 → 경계 변형**이다.

1. 깊이별 밀도 설정으로 방의 개수를 뽑는다.
2. 크기가 다른 타원형·불규칙한 방을 배치한다.
3. 같은 동굴 묶음의 방을 연결 그래프로 묶는다.
4. 구불구불한 통로를 만들고 일부 연결을 추가해 순환 경로를 만든다.
5. 별도 묶음은 고립된 공동으로 남길 수 있다.
6. 방과 통로 안의 벽을 제거하되 바닥을 유지한다.
7. 경계에 작은 노이즈나 제한적인 스무딩을 적용한다.
8. 최종 연결성을 확인하고 막힌 통로를 복구한다.

방의 개수뿐 아니라 최종 빈 공간 비율도 제한한다. 얕은 층에는 작은 공동, 깊은 층에는 큰 동굴이 많도록 조절할 수 있다.

모든 동굴을 연결할 필요는 없다. 플레이어가 파서 연결하는 것이 게임의 일부다. 다만 하나의 연결된 동굴로 계획한 묶음은 실제로 연결되어야 한다.

첫 버전은 지역 전체를 한 번에 생성한다. 청크별 지연 생성으로 확장하면 청크보다 큰 구역에서 동굴 계획을 만들고 각 청크에 해당 부분을 적용한다. 청크마다 독립 생성하면 경계에서 통로가 끊길 수 있다.

### 4.5 생성 검증

- 시작 위치에 충분한 보행 바닥이 있는가?
- 초기 계단을 굴착할 수 있는 구역이 있는가?
- 동굴과 암석의 비율이 설정 범위 안인가?
- 필수 자원이 게임 규칙상 접근 가능한 범위에 있는가?
- 원치 않는 구멍이나 계단 연결 오류가 없는가?
- 연결하기로 한 방 사이의 통로가 열려 있는가?

실패하면 해당 부분을 보정하거나 제한된 횟수만 재생성한다. 무한 재시도 대신 실패 원인과 안전한 대체 배치를 준비한다.

강은 후속 확장으로 둔다. 고도를 따라 흐르는 물의 방향·유량과 호수·배수 규칙이 필요하다.

## 5. 휠 층 전환과 3/4 화면

### 5.1 단면 표시 규칙

- `viewZ`보다 높은 지형은 숨긴다.
- 선택 높이의 벽은 암석·흙 단면으로 표시한다.
- 그 위치에 벽이나 바닥이 없으면 아래쪽에서 보이는 표면을 찾는다.
- 아래층은 어둡게 표시하고 높이 차이를 알려준다.
- 미탐사 공간은 내부 구조 대신 가림 타일을 표시한다.
- 아래층 표면을 클릭했다면 그 표면의 실제 논리 좌표를 사용한다. 모든 클릭을 `viewZ`로 해석하지 않는다.

```text
휠 입력
  → viewZ를 월드 범위 안에서 변경
  → 해당 높이의 표시 데이터 요청
  → 카메라에 보이는 바닥·벽·단면 갱신
```

초기에는 X/Y 화면 격자를 유지하고 타일의 윗면·앞면·음영으로 입체감을 표현한다. 반드시 다이아몬드형 아이소메트릭일 필요는 없다. 층마다 화면 오프셋을 넣는다면 클릭 역변환과 가림 규칙도 함께 수정한다.

### 5.2 표시 레이어

| 레이어 | 내용 |
|---|---|
| 지면 | 잔디·흙·모래·동굴 바닥 |
| 벽과 단면 | 암석 벽·절벽 앞면·잘린 재질 |
| 사물과 주민 | 나무·가구·아이템·캐릭터 |
| 오버레이 | 작업 지정·선택·디버그 경로 |
| 탐사 가림 | 미탐사 영역 |

정적 바닥은 Tilemap으로 묶어서 시작한다. 벽·나무·주민의 앞뒤 관계는 별도 정렬 정책을 둔다. 필요하면 주민 주변의 앞쪽 벽을 낮추거나 반투명하게 표시한다.

Unity Tilemap Chunk 모드에서는 타일을 같은 Sprite Atlas에 모으는 것을 고려한다. Atlas만으로 캐릭터와 벽 사이의 모든 정렬 문제가 해결되지는 않는다.

## 6. 채굴과 계단

### 6.1 수평 채굴

```text
벽 지정
  → 같은 층의 인접 작업 가능 칸 수집
  → 도달 가능한 작업 칸 선택
  → 주민 이동
  → 작업 시간 진행
  → 대상과 작업 위치 재검증
  → 벽 제거, 바닥 유지
  → 산출물 생성
  → 표시·탐사·이동 정보 갱신
```

- 벽 자체를 길찾기 목적지로 삼지 않는다. 벽 옆의 작업 칸이 목적지다.
- 접근 불가능한 지정 작업은 대기시킨다. 매 프레임 실패 경로를 재탐색하지 않는다.
- 주민이 여러 명이면 작업 예약으로 중복 채굴을 막는다.
- 벽 제거 후 새로 접근 가능해진 주변 작업을 재평가한다.

### 6.2 계단 굴착

아래층 계단 굴착은 현재 접근 가능한 위 칸에서 시작하는 특수 작업이다. 아직 없는 아래층 계단에 먼저 도착해야 하는 조건을 만들지 않는다.

```text
위 칸 (x, y, z):     StairDown
아래 칸 (x, y, z-1): StairUp
```

완료 시 필요한 벽을 제거하고 두 칸을 함께 갱신한다. 계단이 발판 역할을 하며 양쪽의 대응 플래그가 모두 있을 때만 층 이동을 허용한다. 연속 계단은 한 칸에 Up과 Down이 모두 있을 수 있다.

첫 버전에서는 경사로·점프·추락 이동을 생략한다. 이 규칙이면 높이가 다른 지표 사이도 계단 없이는 걸어갈 수 있으므로 생성과 UI에 반영한다.

## 7. A* 길찾기

### 7.1 원리

```text
F = G + H
G: 출발점에서 해당 칸까지 알려진 최소 이동 비용
H: 해당 칸에서 목적지까지 남은 예상 비용
```

F가 작은 후보부터 탐색한다. 목적지에 가까워지는 방향을 우선하되 벽을 우회하는 경로도 찾는다.

### 7.2 이동 가능한 이웃

- 같은 층의 상하좌우 중 벽이 없고 바닥 또는 유효한 계단 발판이 있는 칸.
- 위로 이동: 현재 칸에 StairUp, 위 칸에 StairDown이 있고 양쪽이 이동 가능한 경우.
- 아래로 이동: 현재 칸에 StairDown, 아래 칸에 StairUp이 있고 양쪽이 이동 가능한 경우.
- 주민은 우선 탐사된 칸만 경로에 사용한다. 숨은 동굴을 알고 이동하지 않게 한다.
- 주민끼리의 일시적인 충돌·예약은 정적 벽 정보와 분리한다.

### 7.3 휴리스틱

수평·수직 이동이 한 번에 한 축을 1칸만 바꾸고 모든 이동 비용이 `cMin > 0` 이상이라면:

```text
H = cMin × (abs(dx) + abs(dy) + abs(dz))
```

이 값은 계단까지 돌아가는 거리를 생략한 낮은 추정치다. 남은 비용을 과대평가하지 않는 휴리스틱과 올바른 비용 갱신이 최소 비용 경로를 찾는 핵심이다. 위 조건에서는 일관성도 성립한다.

대각선·순간이동·여러 층을 건너뛰는 이동을 추가하면 휴리스틱을 다시 설계한다. H를 0으로 두면 다익스트라 탐색이 되어 유효하지만 더 넓게 탐색할 수 있다.

### 7.4 의사코드

```text
gScore[start] = 0
최소 힙에 (start, g=0, f=H(start)) 추가

힙이 비어 있지 않은 동안:
    F가 가장 작은 항목을 꺼낸다
    항목의 g가 최신 gScore와 다르면 오래된 항목이므로 건너뛴다
    목적지라면 cameFrom을 역추적해 경로를 반환한다

    각 이동 가능한 이웃에 대해:
        nextG = gScore[current] + 이동 비용
        nextG가 기존 비용보다 작으면:
            cameFrom[neighbor] = current
            gScore[neighbor] = nextG
            힙에 최신 g와 f를 넣는다

힙이 소진되면 경로 없음
```

탐색 중 지형이 바뀔 수 있으면 이동 정보의 버전을 확인한다. 주민이 경로를 따라가는 동안에도 다음 칸의 유효성을 검사한다.

### 7.5 규모가 커진 뒤

- 유효한 경로는 재사용한다.
- 경로 요청을 프레임마다 분산한다.
- 도달 불가 결과는 관련 지형 변경 또는 제한된 재시도 조건까지 캐시한다.
- 계단·구역 그래프로 큰 경로를 찾고 구역 내부에서 상세 A*를 수행할 수 있다.
- 통로·계단 변경 시 관련 연결 정보와 경로 캐시를 무효화한다.
- 계층 길찾기는 구현에 따라 전역 최단 경로를 보장하지 않을 수 있으므로 경로 품질을 확인한다.

## 8. 탐사와 동굴 발견

- 실제 월드 데이터, 과거 탐사 여부, 현재 시야를 분리한다.
- 첫 버전에서는 입구가 열리면 같은 층의 연결 공간을 Flood Fill로 발견하는 단순 정책을 사용할 수 있다.
- 이 정책은 굽은 동굴 전체가 한 번에 드러날 수 있다. 엄격한 시야가 필요하면 거리·벽 차폐 기반 FOV를 사용한다.
- 계단 발견만으로 다른 층 전체를 자동 탐사하지 않는다.
- 큰 공간의 Flood Fill은 큐를 유지해 여러 프레임으로 나눈다.
- 휠로 내려봐도 미탐사 정보는 가려져 있어야 한다.

## 9. 최적화

### 9.1 데이터와 표시 분리

- 칸마다 GameObject·Collider·Update를 만들지 않는다.
- 칸은 배열에 저장하고 표시는 Tilemap 또는 청크 메시로 묶는다.
- 격자 주민의 벽·바닥 판정은 칸 데이터 조회로 처리한다.
- 데이터 청크는 예를 들어 32 × 32 × 8칸부터 시험한다.
- 게임의 데이터 청크와 TilemapRenderer 내부 청크는 별개다.
- 데이터 청크마다 반드시 GameObject 하나를 둘 필요는 없다.

메모리 예시:

```text
256 × 256 × 64 = 4,194,304칸
4바이트/칸이면 기본 데이터 약 16MiB
이 문서의 8바이트/칸 예시라면 약 32MiB
```

렌더링·경로 버퍼·캐시·주민·아이템 비용은 별도다. 처음에는 단순 배열을 쓰고 압축·스트리밍은 필요성이 확인된 뒤 적용한다.

### 9.2 변경된 부분만 갱신

```text
CellChanged
  ├─ RenderDirty: 벽·바닥·보이는 표면
  ├─ NavigationDirty: 이동 가능 여부·연결
  ├─ VisibilityDirty: 탐사·차폐
  └─ SaveDirty: 저장 대상 청크
```

- 같은 청크의 여러 변경은 모아서 처리한다.
- 청크 경계를 수정했다면 이웃 청크도 갱신한다.
- 바닥 제거는 같은 X/Y 열의 아래쪽 표시에도 영향을 준다.
- 동굴 개방이나 구역 분리는 영향 범위가 넓을 수 있다. 모든 변경을 항상 몇 칸 갱신으로 끝낼 수는 없다.
- 무거운 작업은 큐에 넣고 프레임별 예산 안에서 처리한다.

### 9.3 휠 전환과 렌더링

- 카메라 안과 여유 범위에 필요한 표면만 그린다.
- 현재 높이와 인접 높이의 표시 결과를 캐시한다.
- 캐시에 메모리 상한을 두고 오래된 결과를 제거한다.
- 지형 변경 시 관련 높이의 캐시를 무효화한다.
- 아래 바닥을 찾기 위해 매 프레임 모든 열을 끝까지 훑지 않는다.
- 빠르게 휠을 돌리면 불필요해진 중간 높이의 표시 작업을 건너뛴다.
- 표시와 클릭 판정 데이터는 같은 버전으로 교체한다.

### 9.4 시뮬레이션 주기

| 대상 | 처리 원칙 |
|---|---|
| 암석·바닥 | 변경 이벤트 때만 |
| 주민 판단·작업 배정 | 예: 초당 5~10회부터 실험 |
| 주민 화면 이동 | 매 프레임 보간 |
| 물·불 | 변화하는 칸과 영향받는 주변 |
| 식생 성장 | 긴 주기 |

화면 밖의 주민도 작업은 계속해야 한다. 렌더링 비활성화와 시뮬레이션 정지를 혼동하지 않는다. 반복 작업에서는 임시 배열·리스트를 대량 할당하지 않고 버퍼를 재사용한다.

### 9.5 Jobs와 Burst

노이즈·지층·동굴 마스크 등 데이터 계산이 실제 병목일 때 적용한다. 처음부터 ECS로 전체를 만들 필요는 없다.

```text
병렬 데이터 계산
  → 완료 결과 큐
  → 메인 스레드에서 Tilemap·GameObject 등 표시 갱신
```

일반적인 Unity 오브젝트 조작을 임의의 워커 스레드에서 실행하지 않는다. 공유 배열 접근, 작업 의존성, 완료 전 읽기, 해제 시점을 관리한다.

## 10. 저장과 불러오기

첫 버전은 지역 전체 스냅샷 저장이 구현하기 쉽다. 변경 청크만 다시 쓰는 방식은 이후 확장할 수 있다.

저장 대상:

- 저장 형식 버전·생성기 버전·월드 시드·생성 설정.
- 칸 상태: 채굴·바닥·계단·건설·액체.
- 주민 위치·작업·아이템·필요한 시뮬레이션 상태.
- 탐사 정보와 플레이 시간.

이후 기본 지형을 시드로 재생성하고 변경분만 저장할 수 있다. 단, 과거 생성기와 설정을 재현할 수 있어야 한다. 그렇지 않으면 청크 스냅샷을 유지한다.

임시 파일에 쓴 뒤 교체하는 저장 방식을 고려한다. 생성기나 저장 구조 변경 시 이전 저장을 어떻게 읽을지도 정한다.

## 11. 권장 코드 구성

아래는 새 Unity 프로젝트에 대한 파일명 제안이다. 현재 구현된 파일 목록이 아니다.

```text
Scripts/
  World/
    WorldData.cs             전체 월드·청크 접근
    ChunkData.cs             칸 배열·변경 버전
    CellData.cs              벽·바닥·계단·재질
    WorldSettings.cs         크기·시드·생성 파라미터
    MaterialDefinition.cs    재질 정의
    BiomeDefinition.cs       기후 조건·식생 정의
  Generation/
    WorldGenerator.cs        생성 순서·검증
    ClimateGenerator.cs      고도·기후
    GeologyGenerator.cs      토양·지층·광맥
    CaveGenerator.cs         방·통로·연결 검사
  Rendering/
    SliceRenderer.cs         현재 높이의 표시
    LayerController.cs       휠·viewZ
    GridProjection.cs        논리 좌표·화면·클릭 변환
  Navigation/
    GridNavigation.cs        이동 규칙·계단 연결
    AStarPathfinder.cs       경로 탐색
    PathRequestQueue.cs      요청 분산
  Simulation/
    AgentController.cs       이동·작업 진행
    MiningSystem.cs          수평 채굴
    StairConstruction.cs     양쪽 계단 연결
    ExplorationSystem.cs     탐사·시야
  Save/
    WorldSaveService.cs      저장·불러오기
```

이동·채굴·저장은 WorldData를 기준으로 처리한다. 렌더러는 게임 데이터의 원본이 아니라 표현 계층이다.

## 12. 집에서 구현할 순서

### A. 고정 테스트 맵

- [ ] 16 × 16 × 4 수동 맵.
- [ ] 벽·바닥·공기·계단 색 표시.
- [ ] 휠로 viewZ 변경.
- [ ] 아래층 표시와 클릭 좌표 확인.

완료 기준: 휠을 돌려도 주민 위치는 바뀌지 않으며 단면과 선택 칸이 일치한다.

### B. 이동과 A*

- [ ] 주민 한 명의 같은 층 이동.
- [ ] 벽을 우회하는 경로.
- [ ] 양쪽 계단 연결을 통한 층 이동.
- [ ] 계단 없는 층은 도달 불가 처리.
- [ ] 바닥 없는 칸 진입 금지.

완료 기준: 주민이 벽·구멍을 통과하지 않고 계단으로만 층을 바꾼다.

### C. 채굴

- [ ] 벽 지정과 인접 작업 칸 선택.
- [ ] 이동 후 벽 제거, 바닥 유지.
- [ ] 위 칸에서 아래층 계단 굴착.
- [ ] 표시·이동·탐사 정보 갱신.

완료 기준: 계단을 내려간 주민이 수평 통로를 만들 수 있다.

### D. 절차적 생성

- [ ] 같은 시드의 생성 결과 일치 확인.
- [ ] 다른 시드에서 결과 변화 확인.
- [ ] 지표·바이옴·지층·광맥 생성.
- [ ] 자연 동굴·통로·고립 공동 생성.
- [ ] 시작 위치와 동굴 비율 검증.

완료 기준: 64 × 64 × 32 지역에서 채굴로 자연 동굴에 도달할 수 있다.

### E. 탐사와 저장

- [ ] 미탐사 동굴 가림.
- [ ] 입구 개방 후 정해진 탐사 정책 적용.
- [ ] 저장 후 지형·주민·계단·탐사 복원.

완료 기준: 휠로 숨은 동굴을 미리 볼 수 없고 파놓은 통로가 재실행 후에도 남는다.

### F. 최적화와 아트

- [ ] 변경 청크만 갱신.
- [ ] 카메라 범위 표시와 인접 높이 캐시.
- [ ] 주민 증가와 경로 요청 분산.
- [ ] 3/4 타일·벽 가림·주민 정렬 적용.
- [ ] Profiler로 병목 확인 후 필요한 부분 확장.

## 13. 검증과 성능 측정

| 상황 | 기대 결과 |
|---|---|
| 동일 시드·설정·생성기 | 초기 칸 데이터 일치 |
| 청크 생성 순서 변경 | 순서 독립 생성 도입 후 같은 결과 |
| 층 빠르게 왕복 | 주민 위치 유지, 표시·클릭 일치 |
| 계단 없거나 한쪽만 존재 | 층 이동 불가 |
| 계단 양쪽 연결 | 정상 이동 |
| 수평 채굴 | 벽만 제거, 바닥 유지 |
| 숨은 동굴 높이 보기 | 미탐사 가림 유지 |
| 동굴 입구 개방 | 정한 정책대로 발견 |
| 경로 중 통로 봉쇄 | 정지 또는 재탐색 |
| 청크 경계 채굴 | 양쪽 표시·이동 정보 갱신 |
| 저장 후 재실행 | 변경 지형과 상태 복원 |

A*는 작은 고정 맵에서 동일 비용이면 BFS, 가중 비용이면 다익스트라의 결과와 경로 비용을 비교한다. 동굴은 연결하기로 한 방들이 실제로 연결되는지 검사한다.

성능은 목표 PC의 실제 빌드에 Profiler를 연결해 측정한다. 60FPS 목표의 전체 프레임 예산은 약 16.7ms이며 월드 갱신만 이 시간을 전부 사용할 수는 없다.

대표 측정 상황:

1. 초기 생성 시간과 최대 메모리.
2. 빠른 층 전환과 카메라 이동.
3. 큰 동굴을 한 번에 발견하는 순간.
4. 주민 1명 → 20명 → 100명의 이동과 작업 판단.
5. 연속 채굴의 타일 갱신량과 GC 할당.
6. 저장·불러오기 시간.

주민 수는 성능 보장이 아닌 부하 시험 단계의 예시다. 평균 FPS뿐 아니라 긴 프레임의 발생 빈도도 기록한다.

## 14. 공식 참고 자료

아래 자료는 Unity 기능을 확인하기 위한 공식 문서다. 이 가이드의 월드 생성·동굴·게임 규칙은 제안 설계이며 마인크래프트나 드워프 포트리스의 내부 구현을 그대로 분석한 결과는 아니다.

- [Unity Tilemap 컴포넌트](https://docs.unity.com/en-us/engine/6000.7/manual/unity2d/tilemaps/reference/tilemap): 격자 기반 타일 표시.
- [Unity: Isometric Tilemap에 높이 추가](https://docs.unity3d.com/6000.0/Documentation/Manual/tilemaps/work-with-tilemaps/isometric-tilemaps/renderer/tilemap-renderer-isometric-modes.html): 높이 표현과 Sprite Atlas 관련 참고.
- [Unity Job System 개요](https://docs.unity3d.com/6000.0/Documentation/Manual/job-system-overview.html): 병렬 데이터 계산 구조.
- [Unity Burst compilation](https://docs.unity3d.com/kr/6000.0/Manual/script-compilation-burst.html): 계산 코드 최적화.
- [Unity Profiler 창](https://docs.unity.com/en-us/engine/6000.3/manual/analysis/profiler/window): 대상 플레이어 성능 측정.

링크마다 Unity 문서 버전이 다를 수 있다. 실제 프로젝트에 설치한 에디터·패키지 버전에 맞춰 API와 설정을 확인한다.

## 15. 가장 먼저 완료할 플레이 흐름

**작은 수동 맵에서 휠로 층을 보고, 주민이 계단으로 내려가 옆 벽을 채굴하는 흐름부터 완성한다.** 이후 같은 데이터 구조를 채우는 절차적 생성기를 붙인다. 이렇게 하면 표시·이동 규칙 문제와 생성 알고리즘 문제를 나누어 해결할 수 있다.
